#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int u;

for (u = 48; u <= 57; u++)
{
putchar(u);
}
for (u = 97; u <= 102; u++)
{
putchar(u);
}
putchar('\n');
return (0);
}
